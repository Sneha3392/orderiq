# OrderIQ — Deployment

> Status: **planned** — completed in the packaging & shipping phase.

Covers Docker images, Docker Compose, Kubernetes manifests (`k8s/`), and the
Terraform / AWS SAM assets (`infra/`).
